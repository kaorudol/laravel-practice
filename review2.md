# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
<input type='hidden' name="_method" value="put">

### findメソッドの引数に指定しているIDは何のIDか
編集したいTodoのid

### findメソッドで実行しているSQLは何か
SELECT * FROM todos WHERE id = ?

### findメソッドで取得できる値は何か
対象のレコード1列分、オブジェクト（デバックする！）

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
ルートパラメータがあるかどうか




## Todo論理削除

### traitとclassの違いとは
トレイトは継承関係に関係なく、好きなクラスに差し込める機能の詰め合わせ 道具としてどこへでも使うことができる。
クラスは実体（インスタンス）を作るための設計図、継承することができる。

### traitを使用するメリットとは
再利用できる
可読性があがる→クラスだといろんなクラスを記述しなければならないため




## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
Routeが実行され、Controllerクラスが呼び出され、インスタンス化されたとき。

### RequestクラスからFormRequestクラスに変更した理由
バリデーションをおこなうため

### $errorsのhasメソッドの引数・返り値は何か
文字列型→inputタグのname属性の値を指定
真偽値（Boolean）→trueがでたら、name属性に対してバリデーションエラーがある
　　　　　　　　　 falseがでたら、バリデーションエラーなし
バリデーションエラーが発生しているか判定

### $errorsのfirstメソッドの引数・返り値は何か
文字列型→formタグのname属性の値を指定
文字列（String） または null（空）
　エラーが出たら文字列、ないときはnullを返す
最初に発生したエラーメッセージを出力

### フレームワークとは何か
ルールはあるが、安全・高速に作れる「土台」。

### MVCはどういったアーキテクチャか
アプリケーションの機能を「3つの役割」に分担させる設計思想（アーキテクチャ）

### ORMとは何か、またLaravelが使用しているORMは何か
ORMとは、プログラミングの上で、オブジェクト指向プログラミングとリレーショナルデータベースの連携を実現するプログラミング技法。ORMを使用することで、データベースの操作を書く際にSQLを直接記述せずに、プログラム言語のオブジェクトを使用して操作を実現できる。
Eloquent：データベースのテーブルをPHPの「モデル（クラス）」として扱い、SQL文を直接書かずに直感的なPHPコードでデータを操作する技術

### composer.json, composer.lockとは何か
Composerとは、PHPのライブラリ管理ツール
Composerを使用することで依存関係にあるライブラリをまとめてインストールすることができます。

composer.jsonは必要なライブラリとバージョン範囲を記述する定義ファイル、
composer.lockは実際にインストールされた全ライブラリの具体的なバージョンを記録する固定ファイル


### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
Vendorディレクトリ

ララベルで使用されてるAPI＝れすとAPI